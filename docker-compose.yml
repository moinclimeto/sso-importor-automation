version: '3.8'
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  gemini-worker:
    build:
      context: .
      dockerfile: worker/Dockerfile
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    env_file:
      - .env
    depends_on:
      - redis
