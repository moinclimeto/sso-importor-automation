const fs = require('fs'); let c = fs.readFileSync('src/pages/EprData.jsx', 'utf8'); c = c.replace(') : activeTab === \\'procurement\\' ? (\\n        records.length', ') : activeTab === \\'procurement\\' ? \\n        records.length'); c = c.replace('          </div>\\n        </div>\\n      ) : (\\n        conversionFactorRecords.length', '          </div>\\n        </div>\\n      ) : conversionFactorRecords.length'); fs.writeFileSync('src/pages/EprData.jsx', c);
